# Modules digits explicitly needs at every boot.
# i2c-dev: /dev/i2c-N userspace devices for i2cdetect/i2cget/i2cset/i2cdump.
#         The base raspi-config package would normally enable this; we purge
#         raspi-config from the image so we have to do it ourselves.
i2c-dev
