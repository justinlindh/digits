# userconf-pi's first-boot user-rename dialog needs a writable rootfs and a
# console; on a Digits phone it can only fail, and its restart loop floods
# the journal and burns CPU. Turn the unit into a no-op.
[Service]
ExecStart=
ExecStart=/bin/true
Restart=no
StandardInput=null
